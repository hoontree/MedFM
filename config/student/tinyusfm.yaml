# TinyUSFM student config for distillation
_target_: model.tinyusfm_seg.SegmentationModel
name: TinyUSFM
num_classes: ${data.num_classes}
checkpoint: checkpoints/TinyUSFM.pth  # Pretrained backbone weights
student_channels: 48