# Knowledge Distillation Configuration (default distill.py entry).
# Common scaffolding (defaults, trainer, checkpoint overrides, hardware, resume,
# output, visualization) lives in config/_distill_base.yaml; only what differs
# is set below.
defaults:
  - _distill_base
  - _self_

teacher: sam
student: tinyusfm

hardware:
  gpu_ids: [1]
