# Teacher-strength sweep — teacher = SAM vit_l full-FT, student = TinyUSFM.
# Method set + shared overrides: _teacher_strength_base.yaml.
#
#   uv run tools/run_reliability_ablation.py \
#       --manifest config/sweeps/teacher_strength_vitl.yaml \
#       --workers gpu2:1,gpu2:2,gpu2:3,gpu6:0,gpu6:1

extends: _teacher_strength_base.yaml
group_label: teacher_strength/vit_l

base_overrides:
  - teacher=sam_vit_l
  - data.num_classes=1
