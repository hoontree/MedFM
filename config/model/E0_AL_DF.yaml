# @package _global_
# SAM: Encoder frozen + Alignment layer + Decoder fine-tuned (E0_AL_DF)

defaults:
  - _self_
  - /training: sam_training

trainer:
  _target_: trainers.sam_trainer.SAMTrainer

model:
  _target_: model.sam_hybrid_adapter.build_sam_hybrid
  name: E0_AL_DF
  sam_type: vit_b
  num_classes: ${data.num_classes}
  img_size: ${data.img_size}
  adaptation_mode: encoder_frozen_alignment_decoder_ft
  r_e: 4
  r_d: 4
  sam_checkpoint: checkpoints/sam_vit_b_01ec64.pth
  alignment_num_blocks: 4
  alignment_hidden_channels: 256
  checkpoint: null  # Set to path to load trained weights
