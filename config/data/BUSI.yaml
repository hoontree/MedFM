name: "BUSI"
type: Combined
num_classes: 2  # Binary segmentation (background, lesion)
img_size: 224
path:
  root: "/home/hheo/data/Ultrasound/seg/breast/BUSI"
  split:
    train: "train"
    val: "valid"
    test: "test"
    vis:
      samples: 16
      interval: 10
extensions: [".png"]
classes:
  benign: "benign"
  malignant: "malignant"
  normal: "normal"
seed: 42
image_suffix: ""
mask_suffix: "_mask"
combine_multiple_masks: true  # Some images have multiple masks (e.g., _mask.png, _mask_1.png)
normalization: "min_max" # Options: "imagenet", "min_max"

# Dataset info
# Total files: benign (891), malignant (421), normal (266)
# Images per class: benign (437), malignant (210), normal (133)
# Some images have 2 masks (16 in benign class) - need to combine them
# Image sizes vary (e.g., 323x473, 500x500)
# Binary masks (background: 0, lesion: 1)