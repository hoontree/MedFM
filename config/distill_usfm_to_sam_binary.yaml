# Knowledge Distillation Configuration: TinyUSFM (teacher) -> SAM (student) — binary.
# Shared scaffolding: config/_distill_base.yaml. This direction uses feature-CWD
# distillation instead of the base's `unified` method.
defaults:
  - _distill_base
  - override /method: cwd_feature
  - _self_

teacher: tinyusfm
student: sam

hardware:
  gpu_ids: [2]
